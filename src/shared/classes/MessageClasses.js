import { cls } from '../cls.js';

export const MessageClasses = (props, colors, { darkClasses }) => {
  return {
    messageSent: {
      ios: 'mr-3',
      material: 'mr-2',
    },
    messageName: {
      ios: 'mr-3',
      material: 'mr-4',
    },
  };
};
