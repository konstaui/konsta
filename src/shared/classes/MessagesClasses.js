import { cls } from '../cls.js';

export const MessagesClasses = (props, colors, { darkClasses }) => {
  return {
    base: {
      common: 'flex flex-col relative z-1 min-h-[100%] px-4 mt-2.5',
   },
  };
};
